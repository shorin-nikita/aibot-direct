# Override для публичного режима (с доменами/SSL)
# Скачано из https://github.com/coleam00/local-ai-packaged

services:
  analytics:
    ports: !reset null

  kong:
    ports: !reset null

  supavisor:
    ports: !reset null
